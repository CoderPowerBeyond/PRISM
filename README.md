# Prism

Clean workspace for the Prism project assets.

## Structure

- `model/`: only Prism model code entrypoints
- `configs/`: configuration files
- `data/`: datasets or data links
- `scripts/`: clean runnable scripts
- `results/`: outputs and figures
- `checkpoints/`: trained model weights

This folder is intentionally minimal and avoids unrelated experimental clutter.
